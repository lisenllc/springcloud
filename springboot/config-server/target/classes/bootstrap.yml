spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/lisenllc/config
          searchPaths: config
          label: master
          username: lisenllc
          password: llc284740336
